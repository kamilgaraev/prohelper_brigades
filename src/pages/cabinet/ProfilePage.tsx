import { BrigadeProfileForm } from '@/components/BrigadeProfileForm';

export function ProfilePage() {
  return (
    <BrigadeProfileForm
      title="Профиль бригады"
      subtitle="Карточка бригады, которую подрядчики увидят в каталоге после одобрения."
    />
  );
}
